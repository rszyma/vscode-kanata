{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build - Debug",
            "type": "shell",
            "command": "just build",
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "group": "build",
        }
    ]
}